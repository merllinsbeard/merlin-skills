# Merlin Skills v0.1.1

Promotes gstack `office-hours` from hidden runtime dependency to a top-level installable skill.

## Included

- Added `skills/office-hours/SKILL.md` copied verbatim from `garrytan/gstack`.
- Kept `gstack-runtime/office-hours/SKILL.runtime.md` for hardcoded upstream gstack paths.
- Updated routing so early product ideas and "is this worth building?" questions go to `office-hours`.
- Kept `plan-ceo-review` as the founder/scope review after a concrete plan exists.
- Updated README, AGENTS, manifest, docs, installer, validation, and changelog.

## Verified

```bash
npm test
SKILL_ROOT=/tmp/merlin-skills-root GSTACK_ROOT=/tmp/merlin-gstack-runtime MERLIN_SKIP_GSTACK_BUILD=1 bash scripts/install-local.sh
```

The temp install produced 24 installable skills and preserved the gstack runtime `office-hours/SKILL.md` shim.
